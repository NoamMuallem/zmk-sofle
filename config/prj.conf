# --- ZMK project config ---

# ZMK Studio live editing
CONFIG_ZMK_STUDIO=y

# Enable combos (two keys pressed together)
CONFIG_ZMK_COMBO=y

# Helpful headroom for heavy hold-tap usage (safe defaults)
CONFIG_ZMK_BEHAVIOR_HOLD_TAP_MAX_HELD=16
CONFIG_ZMK_BEHAVIOR_HOLD_TAP_MAX_CAPTURED_EVENTS=64
